{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && ( prisma db push --skip-generate --accept-data-loss || echo 'db push skipped' ) && tsx prisma/ensureSeed.ts && next build",
  "crons": [
    {
      "path": "/api/cron/purge-reservations",
      "schedule": "0 6 * * *"
    }
  ]
}
